listA = [1, 2, 3]
listB = [1, "2", 3.0] #Python treats mixed-type lists as a string.
#In a traditional array, all of the values must be the same data type so that they are the same size.
#Lists are useful because they can handle mixed data types, are easy to understand and are easy to manipulate.

print(listA)
print(listB)